<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions merged from Shizuku Manager + Canta -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission
        android:name="android.permission.QUERY_ALL_PACKAGES"
        tools:ignore="QueryAllPackagesPermission" />
    <uses-permission
        android:name="android.permission.WRITE_SECURE_SETTINGS"
        tools:ignore="ProtectedPermissions" />

    <uses-feature
        android:name="android.software.leanback"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.touchscreen"
        android:required="false" />

    <!-- Custom permission group for Shizuku API consumers (kept from Shizuku) -->
    <permission-group
        android:name="com.rezuku.pk.permission-group.API"
        android:description="@string/permission_group_description"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/permission_group_label" />

    <permission
        android:name="com.rezuku.pk.permission.API_V23"
        android:description="@string/permission_description"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/permission_label"
        android:permissionGroup="com.rezuku.pk.permission-group.API"
        android:protectionLevel="dangerous" />

    <queries>
        <package android:name="moe.shizuku.privileged.api" />
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <category android:name="android.intent.category.BROWSABLE" />
            <data android:scheme="https" />
        </intent>
    </queries>

    <application
        android:name=".RezukuApplication"
        android:allowBackup="true"
        android:autoRevokePermissions="allowed"
        android:banner="@mipmap/ic_launcher"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_descriptor"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:enableOnBackInvokedCallback="true"
        tools:targetApi="35">

        <!-- Main entry point: a Compose host that navigates between
             Shizuku-manager screens and Canta's app-list screens. -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- ADB pairing foreground service (from Shizuku) -->
        <service
            android:name=".shizuku.adb.AdbPairingService"
            android:exported="false"
            android:foregroundServiceType="shortService" />

        <!--
          NOTE: Shizuku's manager app also declares a RequestPermissionActivity here —
          the dialog shown to a *third-party* app when it requests this app's Shizuku
          permission (relevant because we host a ShizukuProvider below). It was
          intentionally left out of this manifest: porting it pulls in a databinding
          layout, an AppActivity base class, and several manager-internal resources
          that don't exist in this tree yet. Declaring the activity without the class
          would fail manifest merging. See README "Known follow-ups".
        -->

        <!-- Shizuku API provider (so other apps can request Shizuku permission from us) -->
        <provider
            android:name="rikka.shizuku.ShizukuProvider"
            android:authorities="${applicationId}.shizuku"
            android:enabled="true"
            android:exported="true"
            android:multiprocess="false"
            android:permission="android.permission.INTERACT_ACROSS_USERS_FULL" />
    </application>
</manifest>
