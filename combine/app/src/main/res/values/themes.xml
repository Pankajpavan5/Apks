<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- App-level theme used by activities that haven't switched into Compose yet.
         Kept minimal so Compose can take over as soon as setContent { } runs. -->
    <style name="AppTheme" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>

    <style name="AppTheme.Dark" parent="Theme.Material3.Dark.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>

    <!-- Theme used by the permission grant and legacy shell request screens.
         These need a fully transparent system bar so they overlay the calling app. -->
    <style name="GrantPermissions" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@color/black</item>
    </style>
</resources>
